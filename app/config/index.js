const messageConfig = require('./message')
const databaseConfig = require('./database')
const responseConfig = require('./responses')

module.exports = {
  messageConfig,
  databaseConfig,
  responseConfig
}
