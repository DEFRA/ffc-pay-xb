name: '$(Build.SourceBranchName)$(System.PullRequest.SourceBranch)$(Rev:.r)'

trigger:
  - main
  - feature/*
  - release/*

pr:
  - main
  - releases/*

parameters:
  - name: prDeployment
    type: boolean
    default: true
  - name: branchDeployment
    type: boolean
    default: true
  - name: sndDeployment
    type: boolean
    default: true

resources:
  repositories:
    - repository: templates
      endpoint: DEFRA
      type: github
      name: defra/fcp-pipeline-common
      # ref: branch-name

extends:
  template: fcp-common-pipeline.yaml@templates
  parameters:
    appFrameworkType: nodejs
    prDeployment: ${{ parameters.prDeployment }}
    branchDeployment: ${{ parameters.branchDeployment }}
    sndDeployment: ${{ parameters.sndDeployment }}
    variableGroupName: ffc-pay-xb-env-var
    pr_params:
      mssqlDb: $(pr_mssqlDb)
      mssqlHost: $(pr_mssqlHost)
      mssqlUser: $(pr_mssqlUser)
    snd2_params:
      mssqlDb: $(snd2_mssqlDb)
      mssqlHost: $(snd2_mssqlHost)
      mssqlUser: $(snd2_mssqlUser)
    dev_params:
      mssqlDb: $(dev_mssqlDb)
      mssqlHost: $(dev_mssqlHost)
      mssqlUser: $(dev_mssqlUser)
    test_params:
      mssqlDb: $(test_mssqlDb)
      mssqlHost: $(test_mssqlHost)
      mssqlUser: $(test_mssqlUser)
    pre_params:
      mssqlDb: $(pre_mssqlDb)
      mssqlHost: $(pre_mssqlHost)
      mssqlUser: $(pre_mssqlUser)
    prd_params:
      mssqlDb: $(prd_mssqlDb)
      mssqlHost: $(prd_mssqlHost)
      mssqlUser: $(prd_mssqlUser)
